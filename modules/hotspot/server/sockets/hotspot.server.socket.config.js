'use strict';

// Create the chat configuration
module.exports = function (io, socket) {

  // socket.on('hotspotAction', function (action) {
  //   action.timestamp = Date.now();
  //   action.user = socket.request.user;
  //
  //   io.emit('hotspotAction', action);
  // });
};
